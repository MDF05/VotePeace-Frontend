# Disclaimer

**VotePeace Frontend** is a user interface provided "as is". 

While we have implemented standard security practices (client-side validation, auth checks), front-end code runs on the client device and can be manipulated. Never trust client-side validation alone; always ensure your backend API is secure.

The authors are not responsible for any misuse of this software.
